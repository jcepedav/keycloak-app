apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  labels:
    app.openshift.io/runtime: postgresql
spec:
  serviceName: postgresql-service
  selector:
    matchLabels:
      app: postgresql
  replicas: 1
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
        - name: postgresql-db
          image: postgres:15
          volumeMounts:
            - mountPath: /data
              name: cache-volume
          env:
            - name: POSTGRES_USER
              value: rhbk
            - name: POSTGRES_PASSWORD
              value: rhbk
            - name: PGDATA
              value: /data/pgdata
            - name: POSTGRES_DB
              value: rhbk
      volumes:
        - name: cache-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
spec:
  selector:
    app: postgresql
  type: LoadBalancer
  ports:
  - port: 5432
    targetPort: 5432
